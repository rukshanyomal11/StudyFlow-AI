export const GoalFields = [
  "userId",
  "title",
  "deadline",
  "status"
];

const Goal = {
  name: 'Goal',
  status: 'scaffold',
  fields: GoalFields,
  note: 'Replace this placeholder with a full Mongoose schema.',
};

export default Goal;
